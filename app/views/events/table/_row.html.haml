%tr
  %td= link_to event.name, event
  %td
    = event.start_time
    - if event.end_time
      to
      = event.end_time
  %td
    = simple_format event.description
  %td.collapsing.right.aligned
    = link_to "Edit", edit_event_path(event), class: 'ui button'