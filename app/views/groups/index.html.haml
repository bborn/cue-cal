-# .ui.right.floated.header.margin-x-zero
-#   = link_to "New Group", new_group_path, class: "ui button"  



=turbo_frame_tag :groups do
  .ui.left.floated.header
    Groups

  .ui.right.floated.header.margin-x-zero
    = link_to "New Group", new_resource_path, class: "ui button"  
  
  %table.ui.table.striped
    %thead
      = render partial: "groups/table/header", object: Group

    %tbody
      = render collection: @groups, partial: "groups/table/row", as: :group

