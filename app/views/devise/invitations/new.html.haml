%h2
  Invite a new user

= form_for(resource, as: resource_name, url: invitation_path(resource_name), html: { class: "ui form", method: :post }) do |f|
  = render "devise/shared/error_messages", resource: resource

  .field
    = f.label :name
    = f.text_field :name


  - resource.class.invite_key_fields.each do |field|
    .field
      = f.label field
      = f.text_field field
  
    
  .actions
    = f.submit t("devise.invitations.new.submit_button"), class: "ui primary button"
