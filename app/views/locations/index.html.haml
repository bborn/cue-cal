.ui.right.floated.header.margin-x-zero
  = link_to "New location", new_location_path, class: "ui button"  

.ui.left.floated.header
  Locations



=turbo_frame_tag :locations do
  %table.ui.table.striped
    %thead
      = render partial: "locations/table/header", object: Location

    %tbody
      = render collection: @locations, partial: "locations/table/row", as: :location

